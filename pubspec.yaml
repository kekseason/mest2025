name: app_1
description: "A new Flutter project."
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0' # GÜNCEL SDK UYUMU İÇİN

dependencies:
  flutter:
    sdk: flutter

  # --- FIREBASE ---
  firebase_core: ^3.6.0
  firebase_auth: ^5.3.1
  cloud_firestore: ^5.4.4
  firebase_storage: ^12.3.2
  firebase_messaging: ^15.1.3
  # --- GOOGLE AUTH (EKLENDİ) ---
  google_sign_in: ^6.1.6

  # --- UI & FONTS ---
  google_fonts: ^6.1.0
  
  # --- UTILS ---
  google_generative_ai: ^0.4.6
  http: ^1.2.0
  image_picker: ^1.0.4  
  shared_preferences: ^2.2.2           
  connectivity_plus: ^5.0.1            
  url_launcher: ^6.2.1
  intl: ^0.18.1  # TARİH FORMATLAMA (EKLENDİ)

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/logo.png
    - assets/logo2.png
    - assets/header_bg.png
    - assets/yemek1.png
    - assets/yemek2.png
    - assets/yemek3.png
    - assets/yemek4.png
    - assets/user1.png
    - assets/user2.png
    - assets/mest_logo.png
    # - assets/images/mest_logo.png  (Eğer klasör yapın farklıysa bunu aç)
    